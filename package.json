{
  "name": "youtube-last-read-comment",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "standardx **/*.ts --fix",
    "dev": "nodemon --ext 'ts' --watch 'src/**/*.ts' --ignore 'src/**/*.spec.ts' --exec 'npx webpack'"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/chrome": "0.0.75",
    "@typescript-eslint/eslint-plugin": "^3.6.1",
    "@typescript-eslint/parser": "^3.6.1",
    "nodemon": "^2.0.4",
    "standardx": "^5.0.0",
    "ts-loader": "^9.4.2",
    "typescript": "^3.1.6",
    "webpack": "^5.75.0",
    "webpack-cli": "^5.0.1"
  },
  "eslintConfig": {
    "rules": {
      "no-unused-vars": "off",
      "@typescript-eslint/no-unused-vars": "error"
    }
  },
  "standardx": {
    "parser": "@typescript-eslint/parser",
    "plugins": [
      "@typescript-eslint/eslint-plugin"
    ],
    "env": {
      "webextensions": true
    }
  }
}
